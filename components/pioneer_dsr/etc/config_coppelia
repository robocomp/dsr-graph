CommonBehavior.Endpoints=tcp -p 10300

# Proxies for required interfaces
BatteryStatusProxy = batterystatus:tcp -h localhost -p 10014
CameraRGBDSimpleProxy = camerargbdsimple:tcp -h localhost -p 10096
DifferentialRobotProxy = differentialrobot:tcp -h localhost -p 10004
FullPoseEstimationProxy = fullposeestimation:tcp -h localhost -p 10216
RSSIStatusProxy = rssistatus:tcp -h localhost -p 10017
UltrasoundProxy = ultrasound:tcp -h localhost -p 10015
LaserProxy = laser:tcp -h localhost -p 10003

agent_id = 30 # Change id
agent_name = pioneer_dsr
tree_view = false
graph_view = true
2d_view = true
3d_view = false
robot_real = false

Ice.Warn.Connections=0
Ice.Trace.Network=0
Ice.Trace.Protocol=0
Ice.MessageSizeMax=20004800
